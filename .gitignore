.cache/
dist/
tmp/

config.yaml

webview.log
vscode.log
# hunkwise
.vscode/hunkwise/

# ReCode local database
.recode

# CCE (code-context-engine)
# CCE local cache (per-machine, not for version control)
.cce/
# Claude Code local settings/hooks written by cce init (machine-specific)
.claude/
# .mcp.json contains absolute paths regenerated by `cce init`
.mcp.json
